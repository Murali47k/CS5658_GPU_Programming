# GPU Programming CourseWork

This repository contains the coursework, assignments, and projects completed as part of the **GPU Engineering** course in Jul-Dec 2026.

---

*Note: All code is fully runnable in **Google Colab** with GPU runtime enabled — just use `%%writefile` to save the `.cu` file and `!nvcc -o output filename.cu && ./output` to compile and run, with no local CUDA setup required*.

